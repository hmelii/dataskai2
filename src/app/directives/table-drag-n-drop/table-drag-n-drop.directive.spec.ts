import { TableDragNDropDirective } from './table-drag-n-drop.directive';

describe('TableDragNDropDirective', () => {
  it('should create an instance', () => {
    const directive = new TableDragNDropDirective();
    expect(directive).toBeTruthy();
  });
});
