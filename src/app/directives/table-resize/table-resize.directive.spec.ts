import { TableResizeDirective } from './table-resize.directive';

describe('TableResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TableResizeDirective();
    expect(directive).toBeTruthy();
  });
});
